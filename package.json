{
    "name": "control-room-skill",
    "version": "0.1.0",
    "private": true,
    "description": "Deterministic state engine for the ControlRoom Codex skill",
    "type": "commonjs",
    "engines": {
        "node": ">=22.18"
    },
    "scripts": {
        "test": "node --test tests/*.test.ts",
        "typecheck": "tsc --noEmit",
        "check": "npm run typecheck && npm test"
    },
    "devDependencies": {
        "@types/node": "22.20.1",
        "typescript": "5.9.3"
    }
}
